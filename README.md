# REO

Set enviormental variable REO as

`cd reo/src`

`echo "export REO=$PWD" >> ~/.bashrc`

`source ~/.bashrc`

`echo "export PATH=\$PATH:$REO" >> ~/.bashrc`

`source ~/.bashrc`

Set enviormental variable HYPRE_DIR to the installation directory of HYPRE library
